# CYBERCOM CTF 2026 - Environment Configuration Template
# ========================================================
# SECURITY CRITICAL: Copy this file to .env and change ALL passwords
# Never commit .env to version control!

# Database Configuration
# IMPORTANT: Change these passwords before deploying to production
MARIADB_ROOT_PASSWORD=changeme_root_password
MARIADB_PASSWORD=changeme_user_password

# Default values (can be customized if needed)
MARIADB_USER=ctfd
MARIADB_DATABASE=ctfd

# Deployment Notes:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with secure passwords: nano .env
# 3. Verify .env is in .gitignore (it should be)
# 4. Run: docker compose up -d
